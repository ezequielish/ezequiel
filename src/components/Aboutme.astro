<section id="section-aboutme" data-section="aboutme">
    <h1>Acerca de mí</h1>

    <p>
        ¡Hola! Mi nombre es Ezequiel me apasiona crear cosas que vivan en la
        web, Comenze por allá en los 2014 en la universidad probando cosas con
        Dreamweaver y PHP.
    </p>
    <p>
        Estoy comprometido con el aprendizaje continuo y siempre estoy buscando
        nuevos desafíos para mejorar mis habilidades.
    </p>
    <p>
        Cuando no estoy frente de la computadora 🖥️ me gusta salir hacer
        ejercicio 🏋🏾‍♀️️, practicar deporte ⚾️ y jugar de vez en cuando 🎮️.
    </p>
    <p>
        Aquí hay algunas tecnologías con las que he estado trabajando
        recientemente:
    </p>
    <ul class="aboutme__list">
        <li>Reactjs</li>
        <li>Nextjs</li>
        <li>Vuejs</li>
        <li>Nuxtjs</li>
        <li>Postgresql</li>
        <li>Mysql</li>
        <li>Nodejs</li>
        <li>Laravel</li>
    </ul>
</section>

<style>
    .aboutme__list {
        display: grid;
        grid-template-columns: 1fr 1fr;
        align-content: center;
        align-items: center;
        padding: var(--pm-size-normal);
    }

    .aboutme__list li {
        position: relative;
        padding-left: var(--pm-size-xsmall);
        line-height: 2;
        font-size: var(--font-size-small);
    }
    .aboutme__list li::before {
        content: "▹";
        position: absolute;
        left: -12px;
        top: -6px;
        color: var(--blue-light);
        font-size: var(--font-size-normal);
    }

    h1 {
        font-size: var(--font-size-medium);
        font-weight: bold;
        position: sticky;
        top: 0;
        z-index: 20;
        background: var(--background-dark);
    }

    h1 span {
        font-size: var(--font-size-small);
        color: var(--blue-light);
        margin-right: 8px;
    }
    p {
        margin-top: var(--pm-size-normal);
        color: var(--grey);
        font-size: var(--font-size-small);
    }

    @media screen and (min-width: 1024px) {
        #section-aboutme {
            grid-column: 2;
        }

        h1 {
            position: relative;
            z-index: 0;
            background: none;
        }
        p {
            font-size: var(--font-size-normal);
        }
    }
</style>
